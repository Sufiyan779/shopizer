pipeline{
    agent {label 'OPENJDK-11-2'}
    triggers{
        pollSCM('* * * * *')
    }
 stages{
    stage('pull from vcs') {
        steps{
            git url: 'https://github.com/Sufiyan779/shopizer.git',
            branch: 'SPRINT_1_DEV'
        }

    }
    stage('build'){
        steps{
            sh 'mvn package'
        }
    }
 }   
}